export class SystemInformationModel {
  /**
   * The full URL (including path) for the legacy odyssey instance.
   */
  legacyServerInstanceUrl: string;

  /**
   * The URL (excluding path) for the legacy odyssey instance.
   */
  legacyServerUrl: string;
}
